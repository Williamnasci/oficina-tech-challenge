# Local host access to the PostgreSQL container exposed by docker-compose.
DATABASE_URL="postgresql://postgres:supersecretpassword@localhost:15432/oficina_db?schema=public"
PORT=3000
JWT_SECRET=change_me
JWT_EXPIRES_IN=1d
CORS_ORIGIN=*
